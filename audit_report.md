# Audit Report

## 1. 审计目标

本项目对处理后的钢板缺陷检测数据集进行数据质量审计，重点覆盖以下四类问题：

1. 检查同一张或高度相似图片是否同时出现在 train 和 test  
2. 检查类别标签是否存在疑似错误、混淆或命名不一致  
3. 检查标注框是否存在明显偏移、过大、过小或漏标  
4. 检查是否存在文件名、背景、图像尺寸等“捷径特征”，使模型可能学习到与缺陷无关的信息



## 2. 审计方法

### 2.1 重复图与高相似图审计

为检查不同数据子集之间是否存在数据泄漏，采用两级方法：

- **精确重复检查**：使用 MD5 哈希比较 train / val / test 之间的图像文件内容
- **高相似图检查**：使用 pHash 哈希，对跨 split 图像进行近似重复匹配，阈值设置为 `pHash distance <= 4`

输出文件：

- `data/processed/meta/audit_exact_duplicates_across_splits.csv`
- `data/processed/meta/audit_near_duplicates_across_splits.csv`



### 2.2 类别标签审计

类别标签审计包括两个层面：

1. **类别命名一致性检查**
   - 统计所有类别名称
   - 检查是否存在拼写不一致、异常字符、孤立类别名等问题

2. **疑似错标或混淆检查**
   - 对尾部类别进行抽样复核
   - 对视觉证据较弱、边界不清晰的类别进行抽样复核
   - 重点关注 `8_yahen`、`9_zhehen`、`10_yaozhe`、`6_siban`

输出文件与证据目录：

- `figures/audit_examples/suspect_labels/`



### 2.3 标注框质量审计

标注框质量审计主要包含以下几个方向：

1. **过大框检查**
   - 计算 `bbox_area_ratio = bbox_area / image_area`
   - 将 `bbox_area_ratio > 0.5` 的样本标记为可疑大框

2. **过小框检查**
   - 统计 bbox 宽、高、面积分布
   - 按规则 `min(w, h) < 20` 过滤训练价值过低的极小框

3. **偏移与粗标注检查**
   - 对原始抽样图和大框样本进行人工复核
   - 检查是否存在框明显偏离目标、覆盖背景过多的问题

4. **漏标检查**
   - 将“多框高密度图像”与“包含可疑大框的图像”作为高风险候选
   - 导出候选图像供人工复核

输出文件与证据目录：

- `data/processed/meta/flagged_large_boxes.csv`
- `data/processed/meta/removed_small_boxes.csv`
- `data/processed/meta/audit_missing_label_candidates.csv`
- `figures/audit_examples/large_boxes/`
- `figures/audit_examples/missing_label_candidates/`



### 2.4 捷径特征审计

捷径特征审计从以下三个角度进行：

1. **文件名模式**
   - 提取文件名前缀，检查其是否高度集中于某一 split

2. **图像尺寸**
   - 统计 train / val / test 的尺寸分布
   - 检查是否存在某些 split 对应特定尺寸

3. **背景与纹理相似性**
   - 结合 pHash 高相似图结果，分析是否存在同一来源批次、相邻区域、相似背景纹理被分散到不同 split 的现象

输出文件：

- `data/processed/meta/audit_shortcut_filename_patterns.csv`
- `data/processed/meta/audit_shortcut_image_sizes.csv`
- `data/processed/meta/audit_shortcut_split_size_summary.csv`



## 3. 审计结果

### 3.1 同图与高相似图跨 split 检查

审计结果如下：

- 跨 split **完全重复图像数：0**
- 跨 split **高相似图像对数：76**

这表明当前数据划分中不存在直接的“完全重复泄漏”；但存在一批高相似样本，可能来源于：

1. 同一钢板表面的相邻区域  
2. 同一采集批次的高相似纹理  
3. 重复采样或视角高度接近的样本  

因此，数据集中不存在明显的泄漏。

对应证据图目录：

```
figures/audit_examples/leakage_pairs/
```



### 3.2 类别标签是否疑似错误或混淆

原始类别统计中发现以下问题：

- `10_yaozhed` 与 `10_yaozhe` 实际属于同一类，但命名不一致
- 存在异常类别 `d`，仅出现 1 次

清洗阶段已采取如下修复：

- 删除异常类别 `d`
- 将 `10_yaozhed` 统一更正为 `10_yaozhe`

修复后，类别体系恢复为 10 类。

进一步对尾部类别和疑似弱缺陷类别进行抽样复核后，可得以下结论：

- 未发现大规模系统性类别互换的直接证据

- 个别样本存在缺陷边界模糊、视觉证据较弱、类别区分困难的问题

  （如`figures/audit_examples/suspect_labels/6_siban_05_img_01_4406743300_00019.jpg`）

  （如`figures/audit_examples/suspect_labels/8_yahen_00_img_03_4405964900_00010.jpg`）

对应证据图目录：

```
figures/audit_examples/suspect_labels/
```



### 3.3 标注框是否明显偏移、过大或漏标

#### 3.3.1 过大框

基于：

- `bbox_area_ratio > 0.5`

共标记可疑大框：

- **61 条**

人工抽检表明，大框问题主要有两类：

对应证据图目录：

```
figures/audit_examples/large_boxes
```

1. **粗标注但可接受**：缺陷真实存在，但框覆盖了较大面积背景  （如`figures/audit_examples/large_boxes/026_img_06_3436814000_00679_10_yaozhe.jpg`）
2. **定位错误**（如：`figures/audit_examples/large_boxes/013_img_03_425640000_00634_6_siban.jpg`）  

而不是大规模非法框。



#### 3.3.2 漏标候选

采用以下规则构造漏标高风险候选：

- 多框高密度图像
- 包含可疑大框的图像

最终得到：

- 候选图像数：**104**
- 导出人工复核样例：**30**

对应证据图目录：

```
figures/audit_examples/missing_label_candidates/
```

综合目前的自动筛查与人工观察，本数据集的主要问题更偏向于：

- 边界不精细

- 少量漏标

  而非明确的大规模系统性漏标。



### 3.4 是否存在文件名、背景、图像尺寸等“捷径特征”

#### 3.4.1 图像尺寸

正方形化后，train / val / test 的图像尺寸完全一致：

- 所有图像均为 `2048 × 2048`

因此，不存在明显的尺寸捷径，模型无法通过尺寸差异直接区分数据来源。

#### 3.4.2 文件名模式

对文件名前缀进行统计后发现，高频前缀普遍同时分布在 train、val、test 三个 split 中，未发现某一高频前缀仅集中出现在单一 split 的现象。

这说明：

- 未发现显式的文件名前缀泄漏
- 不存在明显“仅靠文件名模式即可识别 split ”的问题

#### 3.4.3 背景与纹理相似性

尽管未发现显式文件名或尺寸捷径，但结合 pHash 审计结果，跨 split 仍存在 76 对高相似图像，说明：

- 同一采集批次
- 相邻区域
- 相似背景纹理

可能被分散到不同 split 中。

因此，背景与纹理层面仍存在一定的隐性捷径风险，可能导致测试表现略偏乐观。



## 4. 已实施修复与建议

### 4.1 已实施修复

1. 删除异常标签 `d`
2. 统一 `10_yaozhed -> 10_yaozhe`
3. 过滤极小框 4 条
4. 标记面积占比过高的大框 61 条
5. 对跨 split 高相似样本进行潜在泄漏审计
6. 构造漏标高风险候选并导出人工复核样例

### 4.2 后续建议

1. **高相似样本重划分**  

   若后续需要更严格的泛化评估，可基于 pHash 或来源批次信息，将高相似样本约束到同一 split，再重新划分数据集。

2. **大框人工复标**  

   对 `bbox_area_ratio > 0.5` 的样本优先人工复核，并对明显覆盖背景过多的框进行收紧。

3. **尾部类重点复核**  

   对 `8_yahen`、`9_zhehen`、`10_yaozhe` 等尾部类别进行二次抽检，以降低少样本类别中的噪声比例。

4. **漏标候选人工确认**  

   对 `audit_missing_label_candidates.csv` 中的候选图进行继续人工审查，重点关注多缺陷图和大框样本。



## 5. 数据清洗对 mAP 的影响

数据清洗对 mAP 的影响主要体现在监督信号质量的改善。删除异常标签并统一类别命名，有助于减少分类混淆；过滤极小框可降低因微小目标和标注误差导致的定位不稳定；正方形化与统一预处理有助于提高训练稳定性；数据增强则可提升模型对亮度、对比度和轻微几何变化的鲁棒性。另一方面，仍保留的粗标注和偏大框可能限制更高 IoU 阈值下的定位性能。因此，预期本项目的数据清洗会对整体 mAP 产生正向影响，但具体提升幅度仍需通过清洗前后对照训练进一步验证。
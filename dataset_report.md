# Dataset Report

## 0.提交项目概览
```
.
├─.idea
│
├─data
│  └─processed
│      └─meta
│         # 处理后的元数据与统计结果目录
│         # 主要存放：
│         # - cleaned_annotations_stage1.csv：清洗后的标注表
│         # - squared_annotations.csv：正方形化后的标注表
│         # - final_annotations_with_split.csv：最终划分后的总标注表
│         # - split_summary.csv：train/val/test 划分统计
│         # - augmentation_log.csv：增强日志
│         # - 各类审计结果 csv（泄漏、大框、漏标候选、捷径特征等）
│
├─figures
│  ├─audit_examples
│  │  ├─large_boxes
│  │  │  # 可疑大框审计样例
│  │  │  # 用于展示 bbox 过大、覆盖背景过多、疑似粗标注的问题
│  │  │
│  │  ├─leakage_pairs
│  │  │  # 数据泄漏审计样例
│  │  │  # 存放 train/val/test 之间高相似图像对的可视化结果
│  │  │  # 用于说明是否存在潜在软泄漏风险
│  │  │
│  │  ├─missing_label_candidates
│  │  │  # 疑似漏标候选样例
│  │  │  # 由规则筛出的高风险图像，用于人工复核是否存在漏标
│  │  │
│  │  └─suspect_labels
│  │     # 疑似错标/弱标注样例
│  │     # 对尾部类别或视觉证据较弱类别进行抽样检查的可视化结果
│  │
│  ├─augmentation_examples
│  │  # 数据增强前后对比图
│  │  # 用于展示增强策略（翻转、亮度/对比度扰动、旋转、缩放等）
│  │  # 以及增强后 bbox 是否仍然正确
│  │
│  ├─raw_preview
│  │  # 原始数据抽样可视化结果
│  │  # 用于验证 XML 解析是否正确、原始 bbox 是否越界/偏移
│  │
│  └─square_examples
│     # 正方形化前后对比图
│     # 用于展示 padding 到正方形后，图像与 bbox 是否同步正确更新
│
└─scripts #核心复现代码
```

## 1. 数据集概述

本项目选用 GC10-DET 钢板表面缺陷数据集，任务类型为目标检测。原始数据保存在 `data/raw/` 目录下，原始标注采用 Pascal VOC XML 格式。每个 XML 文件包含图像文件名、图像尺寸、类别名称以及一个或多个 bbox。

- 下载来源：[GC10-DET](https://www.kaggle.com/datasets/alex000kim/gc10det?resource=download)

在原始解析阶段，共解析得到 2294 个 XML 文件、3564 条原始标注记录，关联到 2292 张图像。解析结果输出为 `raw_annotation_summary.csv`，字段包括：

- `image_path`
- `image_width`
- `image_height`
- `class_name`
- `class_id`
- `xmin`
- `ymin`
- `xmax`
- `ymax`
- `source_annotation`
- `is_valid_raw`

原始可视化抽检表明，XML 解析逻辑正确，绝大部分 bbox 坐标能够正确映射到图像缺陷区域；但同时发现部分样本存在标注框偏大、定位不够精细、个别样本缺陷不明显等原始标注噪声问题。



## 2. 原始标注问题分析

> 原始样本可视化见 `figures/raw_preview/`

在对原始类别进行统计时，发现类别命名存在不一致与异常标签问题：

- `10_yaozhed` 与 `10_yaozhe` 实际属于同一类缺陷，但命名不一致
- 存在异常类别 `d`，仅出现 1 次，不符合正常类别命名规范

因此在清洗阶段采取如下策略：

1. 删除异常类别 `d`
2. 将 `10_yaozhed` 统一更正为 `10_yaozhe`

此外，原始抽检还发现部分样本存在以下质量问题：

1. 个别 bbox 存在一定偏移

2. 少数样本缺陷不明显，可能弱标注或误标

这些问题会影响模型训练中的定位精度与泛化性能，因此在后续数据审计中需要继续关注。



## 3. 数据清洗策略

### 3.1 非法框过滤

对所有解析出的原始框进行合法性检查，规则包括：

- `xmin < xmax`
- `ymin < ymax`
- 坐标不越界
- 图像尺寸与 bbox 字段非空

结果显示，本数据集中不存在不合法几何框，因此未删除任何样本。

### 3.2 异常类别清洗

- 删除异常类别 `d`：1 条
- 将 `10_yaozhed` 统一更正为 `10_yaozhe`：131 条

清洗后类别体系恢复为 10 类。

### 3.3 小框过滤

首先对 bbox 宽、高和面积进行统计分析，结果显示：

- 最小宽度：14
- 最小高度：16
- 最小面积：576
- 不存在 `min(w,h) < 8`、`min(w,h) < 12` 或 `bbox_area < 64` 的情况

从原始分布看，数据集并不存在典型“极小目标”问题。  

但考虑到后续图像需要正方形化并缩放到固定输入尺寸，原图中短边过小的目标在缩放后可能只剩少量像素，训练价值有限。因此本项目采用将阈值设为20：

- 小框过滤规则：`min(w, h) < 20`

实际共删除 4 条标注。

### 3.4 大框标记

进一步计算 `bbox_area / image_area`，将面积占比超过 50% 的框标记为可疑大框，共发现 61 条。  

这些样本不直接删除，保留到后续审计阶段进行人工复核，避免误删大缺陷样本。



## 4. 正方形化处理

> 正方形化对比见 `figures/square_examples/`（左图为原始图，右图为正方形化图）

为统一图像形状并适配后续检测模型训练，同时尽可能防止截断缺陷区域，采用padding的方式进行预处理至正方形，而非裁剪。

具体做法为：

- 对每张图像按长边补0到正方形
- 根据 padding 偏移量同步修正 bbox 坐标
- 重新记录图像宽高与 padding 参数
- 对正方形化后的标注再次检查，确保其合法

输出结果显示：

- 输入图像数：2292
- 输出标注数：3559
- 正方形化后非法框数量：0



## 5. 数据划分
# 按题目要求，对按类分进行Origin作图
![](https://github.com/Oberonft36/QM-2026-4-Multi-Source-Data-Acquisition-Processing-and-Augmentation-for-Defect-Detection/blob/main/%E6%8C%89%20split%20%E7%BB%9F%E8%AE%A1%20By%20Origin.png)

### 选择YOLO

本项目统一输出为 YOLO txt 格式，而非 COCO json。选择 YOLO txt 的原因在于：其标注结构简单，每张图像对应一个独立的标签文件，便于在清洗、正方形化、数据增强和增强后校验过程中进行逐图检查与更新；同时，本项目最终采用 `images/train|val|test` 与 `labels/train|val|test` 的目录组织方式，YOLO txt 与该结构天然匹配，后续也更方便直接用于目标检测模型训练与可视化调试。相比之下，COCO json 更适合大规模集中式管理，但对本项目这种强调逐图清洗与复核的考核而言，YOLO txt 更轻量、更直接，因此选择将处理后的数据统一导出为 YOLO txt 格式。

本项目采用固定随机种子进行图像级划分：

- 随机种子：42
- 划分比例：7:2:1
- 采用随机划分

划分结果如下：

| 分类  | 图像数 | bbox标注数 | 平均bbox标注数 |
| ----- | -----: | ---------: | -------------: |
| train |   1604 |       2503 |         1.5605 |
| val   |    458 |        694 |         1.5153 |
| test  |    230 |        362 |         1.5739 |

可以看到，三个类别的平均标注数相似，划分结果合理。



## 6. 类别分布统计

按最终有效标注统计，类别由高至低分布如下：

| 类名        | bbox标注数 | 图像数 | bbox占比 |
| ----------- | ---------: | -----: | -------: |
| 6_siban     |        884 |    734 |   0.2484 |
| 5_youban    |        569 |    250 |   0.1599 |
| 2_hanfeng   |        513 |    512 |   0.1441 |
| 4_shuiban   |        354 |    310 |   0.0995 |
| 7_yiwu      |        346 |    201 |   0.0972 |
| 1_chongkong |        326 |    326 |   0.0916 |
| 3_yueyawan  |        265 |    264 |   0.0745 |
| 10_yaozhe   |        143 |    140 |   0.0402 |
| 8_yahen     |         85 |     46 |   0.0239 |
| 9_zhehen    |         74 |     53 |   0.0208 |

可以看出，数据集的有效标注存在类别不平衡：

- `6_siban` 占比达到 24.84%
- `9_zhehen` 仅为 2.08% 

需要数据增强处理



## 7. 图像粒度统计

按每张图中的缺陷数量统计如下：

| 缺陷数量 | 对应图片数量 |
| -------- | -----------: |
| 1 缺陷   |         1496 |
| 2-3 缺陷 |          692 |
| 4+ 缺陷  |          104 |

大多数图像仅包含 1 个缺陷，少部分图像包含多个缺陷。

1. 单缺陷样本占主体，适合基础检测训练
2. 多缺陷样本相对较少，不利于复杂场景泛化



## 8. 数据增强

> 增强样例见 `figures/augmentation_examples/`

对训练集增强，采用的增强策略有：

1. 水平翻转
2. 亮度扰动
3. 对比度扰动
4. 轻微旋转与仿射缩放

增强框架使用 Albumentations，并显式启用 bbox 同步更新。为保证增强后的标注质量，设置如下校验规则：

- 若增强后无有效 bbox，则丢弃该样本
- 若保存后的标签文件为空，则丢弃该样本
- 若 bbox 不满足 YOLO 归一化范围要求，则不写入标签

增强结果如下：

| 状态 | 数量 |
| ---- | ---: |
| 成功 | 1601 |
| 失败 |    3 |

augmentation_log.csv显示：失败样本全部属于增强后 bbox 不再可见的情况，因此被丢弃。



## 9. 清洗与增强阶段数据量变化

| 处理阶段      |  图像数 |  标注数 |
| --------- | ---: | ---: |
| 原始数据解析    | 2292 | 3564 |
| 清洗阶段 1    | 2292 | 3559 |
| 正方形化处理    | 2292 | 3559 |
| 最终划分后总计   | 2292 | 3559 |
| 训练集增强成功样本 | 1601 | 1601 |
| 训练集增强失败样本 |    3 |    无 |


从数据量变化可以看出：

1. 原始清洗阶段删除极少量脏标签与极小框
2. 正方形化未导致标注数减少，仅改变图像尺寸与 bbox 坐标
3. 数据增强扩展了训练样本规模，且失败比例低
